-#
  #url-redirect
    %h3.st.sb
      URL Redirects
    %p.note.ssb
      URL redirects are used to prevent old bookmarks and links to your previous site from breaking. This will automatically forward the link to your target location.
      %br
      Note that the following paths cannot be used: &ldquo;/application&rdquo;, &ldquo;/cart&rdquo;, &ldquo;/carts&rdquo;, &ldquo;/orders&rdquo;, and &ldquo;/shop&rdquo;.
    .section
      #redirects-header.section-header.link-header.redirect-header-add.nobull.st
        %span
          Old path
        %span
          New path
      %ul#redirects.nobull.sr.ssb.sl
      #no-redirect-hint.hint.padding
        No URL redirects added yet.
      #btm-nav.act-screen
        #pagination
      #test-row.action-row
        .action-link
          %a{ :href => "#", :onclick => "Element.togglePair('redirect-table-detail', 'test-row'); return false;" }
            %span.plus
              +
            Add URL redirect
      #redirect-table-detail.btns{ :style => "display:none;" }
        %form{ "accept-charset" => "UTF-8", :action => "/admin/redirects", "data-remote" => "true", :method => "post" }
          %div{ :style => "margin:0;padding:0;display:inline" }
            %input{ :name => "utf8", :type => "hidden", :value => "&#x2713;" }
            %input{ :name => "authenticity_token", :type => "hidden", :value => "q+mFURLfs7+M4sgHJyCH+7FwocVPKvMsmSxB2KYw7K8=" }
          %dl#add-redirect-form.st.sl.sr.sb
            %dt
              %label{ :for => "redirect-table-detail-path" }
                Old path
                %span.note
                  Enter the old path you want redirected.
              %br
          %p.note
            For example, if your old url was
            %span
              www.example.com/shirts
            , you should enter
            %span
              \/shirts
            below.
          %dd
            %input#redirect-table-detail-path.long{ :type => "text", :name => "redirect[path]" }
          %dt
            %label{ :for => "redirect-table-detail-target" }
              New path
              %span.note
                Enter the new path you want to redirect to.
            %br
          %p.note
            For example, if your new url is
            %span
              www.example.com/collections/shirts
            , you should enter
            %span
              \/collections/shirts
            below.
          %dd
            %input#redirect-table-detail-target.long{ :type => "text", :name => "redirect[target]" }
          .st.sl.sr.sb.btn-divider
            %input.btn{ :name => "commit", :type => "submit", :value => "Add URL redirect" }
            or
            %a{ :href => "#", :onclick => "Element.togglePair('redirect-table-detail', 'test-row'); return false;" }
              Cancel

