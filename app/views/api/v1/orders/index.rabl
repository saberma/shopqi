collection @orders, root: :orders, object_root: false
attributes :id, :name, :note, :number, :subtotal_price, :token, :total_line_items_price, :total_price, :total_weight, :order_number, :cancel_reason, :cancelled_at 
